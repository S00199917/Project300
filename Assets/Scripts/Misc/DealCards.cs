using Mirror;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DealCards : MonoBehaviour
{
    

    public void AddCard()
    {
        //int random;
        //for (int i = 0; i < 2; i++)
        //{
        //    random = Mathf.RoundToInt(Random.Range(0, 51));
        //    player1Hand.Add(reusable.cards[random]);
        //    Instantiate(player1Hand[i].card);

        //    random = Mathf.RoundToInt(Random.Range(0, 51));
        //    player2Hand.Add(reusable.cards[random]);
        //    Instantiate(player2Hand[i].card);
        //}

        //DistributeCards(topLeftP1, bottomRightP1, player1Hand.Count, player1Hand);
        //DistributeCards(topLeftP2, bottomRightP2, player2Hand.Count, player2Hand);
    }
}
